# MongoDB Configuration
# Use local MongoDB or MongoDB Atlas (free tier: 512MB)
MONGODB_URI=mongodb://localhost:27017
# OR for Atlas: MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/vidinsight

MONGODB_DB_NAME=vidinsight

# No Firebase needed! Auth is handled locally.
